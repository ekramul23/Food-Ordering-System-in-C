# 🍔 Food Ordering System (C Project)

A simple **console-based food ordering system** written in C.  
This project demonstrates the use of **structures, arrays, functions, and basic file handling concepts** to simulate a restaurant ordering workflow.

---

## 📌 Features
- **Browse Menu** – View available food items with prices.
- **Add Item to Menu** – Add new food items dynamically.
- **Delete Item from Menu** – Remove unwanted items.
- **Replace Item in Menu** – Update existing menu items.
- **Add Items to Cart** – Select items and quantities for your order.
- **Checkout** – Calculate total bill and confirm the order.
- **Order History** – View all past orders with details.
- **Exit** – Quit the program gracefully.

---

## 🛠️ Technologies Used
- **Language:** C
- **Libraries:**  
  - `<stdio.h>` – Input/Output operations  
  - `<string.h>` – String handling  
  - `<conio.h>` – Console input/output (for `getch()` and `system("cls")`)  

---


